from setuptools import setup, find_packages

setup(
    name="company_theme",
    version="0.0.1",
    description="Custom ERPNext desk sidebar theme",
    packages=find_packages(),
    include_package_data=True,
    install_requires=[],
)
